function toggleMobileMenu() {
    const navList = document.querySelector('.nav-list');
    navList.classList.toggle('show');
}

// Example usage of the role system
/*if (isAdmin()) {
    // Admin-specific code for managing blogs and responding to testimonials
    console.log("Admin can manage blogs and respond to testimonials");
} else {
    // Regular user code
    console.log("Regular user can view blogs and testimonials");
}

// Display testimonials and blogs on page load
document.addEventListener('DOMContentLoaded', () => {
    // Call your functions to display testimonials and blogs here
    // displayTestimonials();
    // displayBlogs();
});
*/